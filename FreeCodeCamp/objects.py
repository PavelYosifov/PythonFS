age = 8
print(age.real)  # 8
print(age.imag)  # 0
print(age.bit_length())  # 4 - bits necessary to represent this number in binary format
items = [1, 2]
items.append(3)  # adds 3 to the end of the list
items.pop()  # removes the last element "3" from the list
print(id(items))  # prints the id of list items
